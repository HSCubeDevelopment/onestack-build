# Build output & deps
dist/
.next/
node_modules/
coverage/

# Generated by Next.js on `next build` / `next dev`. It is git-ignored, but Prettier still walks the
# working tree, so `npm run build` followed by `npm run format` fails locally on a file we neither
# wrote nor commit. CI formats before it builds, so it never exists there.
apps/web/next-env.d.ts

# Design source of truth — vendored reference material, not code we maintain.
# prototype.html is exported from the design tool and is read for exact CSS values (card 302);
# reformatting it would create diff noise against the export and risk altering the reference.
design/
