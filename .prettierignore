# Build output & deps
dist/
.next/
node_modules/
coverage/

# Design source of truth — vendored reference material, not code we maintain.
# prototype.html is a design-tool export read for exact CSS values (card 302); reformatting it would
# create diff noise against the export and risk altering the reference.
design/
