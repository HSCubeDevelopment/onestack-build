
# LOCAL ONLY. Stands in the demo OWNER for anonymous callers. Never set this on a deployment —
# it is ignored when NODE_ENV=production, which is the only thing that makes that safe.
DEV_LOGIN_ENABLED=false
