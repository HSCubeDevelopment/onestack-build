'use client';
import { TicketForm } from '@/components/autotech/TicketForm';

/** File a police / infringement ticket against a car (employee flow). */
export default function TicketPage() {
  return <TicketForm />;
}
