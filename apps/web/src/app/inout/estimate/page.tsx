'use client';
import { EstimateFlow } from '@/components/autotech/EstimateFlow';

/** Instant estimate — snap photos of a damaged car → AI draft (fixes, parts, price) to edit on screen. */
export default function EstimatePage() {
  return <EstimateFlow />;
}
