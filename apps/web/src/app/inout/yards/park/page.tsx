'use client';
import { ParkCarForm } from '@/components/autotech/ParkCarForm';

/** Park a car at a yard — the employee-side drop flow. */
export default function ParkCarPage() {
  return <ParkCarForm />;
}
