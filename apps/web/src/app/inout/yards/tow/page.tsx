'use client';
import { TowInForm } from '@/components/autotech/TowInForm';

/** Tow a car in — staff-accessible tow collection; saving creates the job file. */
export default function TowInPage() {
  return <TowInForm />;
}
