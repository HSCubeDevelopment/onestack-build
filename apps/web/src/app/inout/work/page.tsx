import { MechanicWork } from '@/components/autotech/MechanicWork';

/** Log a service — before/after photos and what was done. The mechanics' main screen. */
export default function MechanicWorkPage() {
  return <MechanicWork />;
}
