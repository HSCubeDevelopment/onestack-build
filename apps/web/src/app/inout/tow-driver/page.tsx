import { TowDriverStatus } from '@/components/autotech/TowDriverStatus';

/** Where the tow driver is up to — the office's view of tow work. Staff-accessible. */
export default function TowDriverPage() {
  return <TowDriverStatus />;
}
