'use client';
import { CarsMenu } from '@/components/autotech/CarsMenu';

/** Cars In / Out — the tile menu behind the home "Cars In / Out" button. */
export default function CarsMenuPage() {
  return <CarsMenu />;
}
