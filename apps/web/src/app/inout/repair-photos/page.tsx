'use client';
import { RepairPhotos } from '@/components/autotech/RepairPhotos';

/** Repair photos — enter a rego → add before / during / after photos to the car's job. */
export default function RepairPhotosPage() {
  return <RepairPhotos />;
}
