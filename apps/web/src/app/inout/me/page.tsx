'use client';
import { MyProfile } from '@/components/autotech/MyProfile';

/** My profile — the employee's own details, roster and self-service PIN change. */
export default function MyProfilePage() {
  return <MyProfile />;
}
